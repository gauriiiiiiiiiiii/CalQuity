# ParcelPilot Support Agent: runtime dependencies
openai>=1.40          # LLM client, points at any OpenAI-compatible endpoint
streamlit>=1.36       # chat UI + ops radar
openpyxl>=3.1         # read the .xlsx data pack (no pandas needed for this small set)
pypdf>=4.0            # parse the supplied policy/contract PDFs
python-dotenv>=1.0    # load LLM_API_KEY from .env
pytest>=8.0           # ground-truth unit tests and the eval harness
